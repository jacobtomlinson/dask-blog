[build]
  publish = "_build/dirhtml"
  command = """
    pip install uv && \
    uv pip install --system . && \
    sphinx-build -b dirhtml . _build/dirhtml
  """

[build.environment]
  PYTHON_VERSION = "3.13"
